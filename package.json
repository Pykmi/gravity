{
  "name": "orb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rimraf ./dist",
    "dev": "tsc --watch",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts,.js --fix",
    "start": "es-dev-server --node-resolve --watch --open --app-index index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.2.1",
    "@typescript-eslint/parser": "^6.2.1",
    "es-dev-server": "^2.1.0",
    "eslint": "^8.46.0",
    "live-server": "^1.2.2",
    "rimraf": "^5.0.1"
  }
}
